# Proyecto de Titulación: Ecosistema de Servicios [Tu Título] (Monorepo)

Este repositorio (monorepo) contiene el código fuente completo de mi proyecto de titulación de Ingeniería en TICs, el cual implementa una arquitectura de microservicios (Backend), un API Gateway (Seguridad) y un cliente (Frontend).

---

## 🏛️ Estructura del Repositorio

Este proyecto está dividido en tres componentes principales, cada uno en su propia carpeta:

* **/GODC** (API Gateway - Laravel)
* **/MIC** (Microservicios Backend - Spring Boot/PHP)
* **/MICDC** (Aplicación Cliente - Frontend)

---

## ⚙️ Instalación y Configuración (Para un nuevo desarrollador)

Estas instrucciones son para configurar el proyecto después de clonarlo.

### 1. Prerrequisitos
* Git
* Composer
* PHP (versión de tu proyecto)
* Java (versión de tu proyecto, para Spring)
* Servidor MySQL

### 2. Configuración del API Gateway (`/GODC`)

1.  **Clonar el repositorio:**
    ```bash
    git clone [tu-enlace-de-git]
    cd MIC/GODC
    ```

2.  **Instalar dependencias de PHP:**
    ```bash
    composer install
    ```

3.  **Configurar el Entorno (`.env`)**
    * Copia el archivo de ejemplo: `cp .env.example .env`
    * **¡BORRA CUALQUIER .sql DE TU REPO!** La base de datos se debe crear con migraciones.
    * Abre el archivo `.env` y configura tus credenciales de base de datos **locales**.

    ```ini
    # Configura la BDD principal del Gateway
    DB_CONNECTION=mysql
    DB_HOST=127.0.0.1
    DB_PORT=3306
    DB_DATABASE=laravel_gateway  # El nombre de tu BDD
    DB_USERNAME=root             # Tu usuario local
    DB_PASSWORD=               # Tu clave local (dejar en blanco si no tienes)
    ```

4.  **Generar Claves y Migrar la Base de Datos:**
    ```bash
    php artisan key:generate
    php artisan jwt:secret
    php artisan migrate --seed  # Esto crea las tablas y datos de prueba
    ```

### 3. Configuración de Microservicios (`/MIC`)

1.  **Navegar a la carpeta:**
    ```bash
    cd ../MIC 
    ```
2.  **Configurar el Entorno (`.env`)**
    * Copia el archivo de ejemplo: `cp .env.example .env`
    * Configura el `.env` con las credenciales **para los roles**. (¡Nunca publiques las contraseñas!)

    ```ini
    # --- Credenciales para los roles (ejemplo) ---
    # Un usuario NUNCA debe ver esto. Esto es solo para el servidor.
    DB_USER_ADMIN=tu_usuario_admin_seguro
    DB_PASS_ADMIN=tu_clave_secreta_admin
    
    DB_USER_PROFESOR=tu_usuario_profe_seguro
    DB_PASS_PROFESOR=tu_clave_secreta_profe
    
    DB_USER_REPRESENTANTE=tu_usuario_repre_seguro
    DB_PASS_REPRESENTANTE=tu_clave_secreta_repre
    ```

3.  **Instalar dependencias y migrar:**
    * (Sigue los pasos de instalación para tu proyecto Spring/PHP)
    * `php artisan migrate --seed` (si aplica)

### 4. Configuración del Frontend (`/MICDC`)

1.  **Navegar e instalar:**
    ```bash
    cd ../MICDC
    # (Añade tus comandos de instalación, ej: `npm install` o `flutter pub get`)
    ```

---

## 🚀 Cómo Ejecutar el Proyecto (Desarrollo Local)

Debes tener **3 terminales** abiertas:

1.  **Terminal 1 (API Gateway - GODC):**
    ```bash
    cd MIC/GODC
    php artisan serve --host=0.0.0.0 --port=8000
    ```

2.  **Terminal 2 (Microservicios - MIC):**
    ```bash
    cd MIC/MIC
    # (Comando para iniciar tu servicio Spring o PHP)
    php artisan serve --host=0.0.0.0 --port=8001
    ```

3.  **Terminal 3 (Frontend - MICDC):**
    ```bash
    cd MIC/MICDC
    # (Comando para iniciar tu frontend, ej: `npm start` o `flutter run`)
    ```
